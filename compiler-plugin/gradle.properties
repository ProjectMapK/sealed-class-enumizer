# POM のモジュール別値（共通値はルートの gradle.properties）
POM_NAME=Sealed Class Enumizer Compiler Plugin
POM_DESCRIPTION=Kotlin (K2) compiler plugin that generates enum-like operations (entries / valueOf / label) for sealed class hierarchies.
