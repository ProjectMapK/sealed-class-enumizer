# POM のモジュール別値（共通値はルートの gradle.properties）
POM_NAME=Sealed Class Enumizer Runtime API
POM_DESCRIPTION=Runtime API for sealed-class-enumizer: base types backing the enum-like operations (entries / valueOf / label) generated for sealed hierarchies.
