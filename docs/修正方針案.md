# 修正方針案

結合テスト実装（[テストケース管理](テストケース管理.md)）で発見された、設計資料の仕様と実挙動の乖離（NG 挙動）の記録と、
アプリ側（runtime-api / compiler-plugin / gradle-plugin）の修正方針の案である（発見時点ではアプリ側の変更を
保留して本書へ集約し、方針が確定したものから順次適用する。解決したものは下記方針で欠番化する）。**解決済みの項目は順次欠番とし、記述を全削除する**（番号は再利用しない。
解決内容は反映先ドキュメント・テストと git 履歴に残るため本書には残さない。ただし他資料が参照する項目は、参照が残る間は
記述を保持する）。未解決の各項目は仕様の根拠・実測・修正案・テスト側の暫定対応（ワークアラウンド）を持つ。

| # | 対象 | 概要 | 状態 |
|---|---|---|---|
| 1 | — | — | 欠番 |
| 2 | — | — | 欠番 |
| 3 | — | — | 欠番 |
| 4 | — | — | 欠番 |
| 5 | — | — | 欠番 |
| 6 | — | — | 欠番 |
| 7 | — | — | 欠番 |
| 8 | — | — | 欠番 |
| 9 | — | — | 欠番 |
| 10 | — | — | 欠番 |
| 11 | — | — | 欠番 |
| 12 | compiler-plugin | 基底ファイルが IC ラウンドに入らない 2 形（末端の新規ファイル追加・中間 sealed ファイル単独編集）で、生成 asEnumish の IR ボディが充填されずバックエンド ICE になる（階層不変条件の不成立） | 案確定・修正未着手 |
| 13 | — | — | 欠番 |
| 14 | — | — | 欠番 |
| 15 | — | — | 欠番 |
| 16 | — | — | 欠番 |
| 17 | 言語側 | 生成 Enumish を型引数に持つ typealias を基底の supertype の頭に使うと、エイリアスが先に解決されない配置で言語側が展開しないままバックエンド ICE になる | 原因確定・プラグイン側の対処余地なし |

---

## 12. 基底不在の IC ラウンドで生成 asEnumish の IR ボディが充填されずバックエンド ICE になる

- **仕様（期待）**: 設計00 §1・§5 — 階層不変条件（末端側ファイルだけが編集されても基底ファイルが
  共連れ再コンパイルされ、生成が常に階層 1 単位で行われる）を IC 安全の一級要件とする。
  §5.3 #2（末端追加）は「利用側の網羅性エラーとして正しく現れる」が期待。
- **実測（NG）**: 次の 2 形で決定的に
  `e: java.lang.RuntimeException: Exception while generating code for ... asEnumish ...`
  `Caused by: IllegalStateException: Function has no body: FUN GENERATED[...EnumizeKey] asEnumish`
  のバックエンド ICE になる（再試行でも持続・**clean ビルドで回復**）:
  1. 末端を**新規ファイル**として追加（`data object Baz : SI` の新 .kt 追加 → 初回 IC ラウンドが
     新規ファイル単独で走る）— TC-IC-011（§5.3 #2）
  2. **中間 sealed のファイル単独の ABI 非変更編集**（2 段・3 段チェーンとも、中間ファイルのコメント
     編集で末端側が基底不在ラウンドで再コンパイルされる）— TC-IC-025/060
  - 既存ファイルへの末端追記・末端削除・基底ファイル同時編集（同一ラウンド入り）では**発生しない**。
    設計00 §5.4 が想定していた「連続編集ラウンド」のクラッシュはむしろ再現しない（コメント・private
    追加・基底編集の連続ラウンドはすべて green・entries 非 stale・バイト一致）。
- **推定原因**: KT-86121（多ファイル sealed × プラグイン生成 × IC。2.5.0-Beta1 修正予定）の再現形。
  設計00 §1 の階層不変条件は「同一**ビルド**での再コンパイル」を述べており、その粒度では成立している
  可能性があるが、コンパイラのマルチラウンド IC は**ラウンド粒度では階層の部分集合ビューを許す**
  （設計01 §7.1 がチェッカーに単調性を要求しているのはこのため）。プラグイン構造としては、
  実装ノート §2 の 4（IC ラウンド外基底のアノテーション直接照合フォールバック）により
  **FIR 側の宣言生成は基底不在ラウンドでも成立する**一方、**IR 側のボディ充填が「基底（と生成
  Enumish）が同一ラウンドの IR にあること」を前提**にしており、部分集合ビューへの耐性がチェッカーに
  しか設計されていなかった非対称が破綻点である — 宣言だけが codegen へ渡って
  "Function has no body" に落ちる。
- **修正方針案**:
  1. **短期（プラグイン側緩和）**: IR 生成時、ラウンド内の各生成宣言（asEnumish 等）についてボディ
     未充填を検出したら、基底・生成 Enumish・companion のシンボルを（ラウンド外でも）シンボルテーブル /
     参照経由で解決してボディを構築する。生成対象の末端クラス IR 自体はラウンド内に必ずあるため、
     参照解決さえできればボディは組める。少なくとも ICE を「明示的な診断（再ビルド or clean を促す）」へ
     落とすガードは必須（沈黙のクラッシュは IC 安全の一級要件に対する最悪の形）。
  2. **恒久**: Kotlin 2.5 系の KT-86121 修正で基底共連れがコンパイラ側で保証されるなら、
     マイナーバージョン毎のアーティファクト分割（概要 §7）の下で 2.5 対応版から本問題を解消済みとし、
     2.4 版は 1. のガード + 既知の制限として文書化する。
  3. **文書**: 設計00 §1/§5 の「階層不変条件（実測確認済み）」に本 2 形の除外を追記し、
     §5.3 #2 の期待を「新規ファイル追加の初回ラウンドは既知の ICE（KT-86121）」へ改める。
     回避策（新規末端の追加は基底ファイルと同時編集で保存する・clean で回復する）を known-issue として
     概要 §7 か README 相当に記載する。
- **関連**: 新規ファイルで追加した末端が label 衝突を起こす場合は、フロントエンドの
  ENUMIZE_LABEL_CLASH（末端側チェッカー）が先にコンパイルを止めるため本 ICE には到達しない（実測）。
  本項の再現形は、追加した末端が既存の階層と衝突しない場合に限られる。
- **テスト側の暫定対応**: 仕様どおりのアサートを持つ 2 メソッドを `@Disabled` 化し、`Kt86121Test` が
  両形を「クラッシュすることが expected」の回帰ゲートとして保持する（Kotlin 更新等で解消したら
  テストが fail して検出できる）。

## 17. 生成型への typealias を基底の supertype の頭に使うとバックエンド ICE になる（配置依存）

- **仕様（期待）**: 設計01 §4・§6.2 — 型引数まで一致する `Enumized<SI.Enumish>` の手動宣言は注入を
  スキップし、supertype の表記で扱いを変えない。テストケース管理.md TC-MAN-085。
- **実測（NG）**: `typealias A = Enumized<SI.Enumish>` を介した `@Enumize sealed interface SI : A` が、
  エイリアスが階層より先に解決されない配置（**階層と同一ファイル**・**ソース処理順で後になる別ファイル**）で
  `Backend Internal error: Exception during IR fake override builder`
  （`IllegalArgumentException`。フロントエンド診断は一切出ない）になる。
  エイリアスが先に処理される別ファイル配置（TC-MAN-081）は成立する。
- **原因（実測で切り分け済み）**: typealias の展開先が**プラグイン生成型**（`SI.Enumish`）である場合、
  エイリアスが階層より先に解決されない配置では、解決済み supertype がエイリアスのまま据え置かれ
  `fullyExpandedType` でも展開できない。プラグイン側の注入抑止は raw 追跡（エイリアス自身のスコープでの
  名前解決）へ落として配置に依らず成立しており、二重注入は起きない。残るのは未展開のまま IR へ渡る
  言語側の経路である。生成型を介さない同型（`typealias A = Box<S.Nested>` + `sealed interface S : A`）は
  プラグイン非適用なら同一ファイルでも正常にコンパイルされるため、生成型との組み合わせに固有である。
- **修正方針案**: プラグイン側で塞ぐ手段は無い（注入は既に抑止済みで、未展開の supertype を
  IR へ渡すのは言語側）。専用診断（この配置を検出して報告する）を設ける案はあるが、言語側の制約に対して
  診断カタログを増やす価値は低い。回避策（**基底の supertype の頭にはエイリアスを使わない** —
  型引数としての使用と末端側での使用は配置に依らず成立する）は概要 §7 に既知の制限として記載済みであり、
  本項は言語側の解消をもって欠番化する。
- **テスト側の暫定対応**: 仕様どおりのアサートは `@Disabled` で保持し、実挙動（ICE）を固定する
  回帰ゲートを併置する（SweepTypealiasSupertypeTest。解消したらゲートが fail して検出できる）。

