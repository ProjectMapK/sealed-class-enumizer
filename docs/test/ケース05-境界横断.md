# ケース05-境界横断

本資料は K12（モジュール役割）と K13（プラットフォーム）を正典として担い、
モジュール境界・プラットフォーム境界越しの全観測面（O1〜O6）を検証する。
E-2 の外部観測面と V1-a/c（else 無し when の跨 module 成立と枝形）は本資料が正典。
順序値=ケース03・診断の発火/near-miss=ケース04・可視性規則=ケース02・
IC/ABI 伝播/決定性/binary-swap のビルド駆動面=ケース06 を正典とし、本資料は境界越しの実行時・静的型観測のみ持つ。

## 1. 跨 module 生成 API（未適用消費）

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| XMP-01 | K12=未適用消費 × O1 | entries/asEnumish/label 拡張が未適用側から参照可・entries は同一インスタンス | CrossModuleApiTest#generatedApiIsVisibleWithoutPlugin / #entriesReturnSameInstance | V1 |
| XMP-02 | K12=未適用消費 × O1 | valueOf/valueOfOrNull が跨 module で解決・失敗文言 `No enumish entry with label 'X' in SI` | CrossModuleApiTest#valueOfResolvesWithDocumentedFailureMessage | V1 |
| XMP-03 | K12=未適用消費 × O4 | enumizedClass が `KClass<out SI>` に共変型付け・enumishCompanion 経由で階層到達し `List<SI.Enumish>` に型付く | CrossModuleApiTest#enumizedClassIsCovariantlyTyped / #enumishCompanionNavigatesHierarchy | — |
| XMP-04 | K12=未適用消費 × O1 | IR-only kind toString が跨 module で仮想ディスパッチ・data object は言語合成のまま | CrossModuleApiTest#kindToStringDispatchesVirtually | V11 |
| XMP-05 | K12=未適用消費 × K3/K11/K15 × O1 | kind API の末端種別横断観測: enum の label/name 管轄分離・型パラメータ末端の kind 型引数非依存・value class の boxing 非依存・enumizedClass の reflection 接続 | CrossModuleApiTest#enumLeafKindIsObserved / #typeParameterizedLeafSharesKind / #valueClassLeafKeepsKindIdentity / #enumizedClassConnectsReflection | V4/V10・種別毎の挙動正典=ケース01 |
| XMP-06 | K12=未適用消費 × O3 | entries 順序（FQN 順・中間 break 順・internal 中間経由）が跨 module で保存 | CrossModuleOrderTest#mixedPlacementKeepsFqnOrder / #intermediateBreakOrderIsPreserved / #internalIntermediateHierarchyIsObservable | 順序値の正典=ケース03・IC 増減面=ケース06（旧 TC-GAP-016） |
| XMP-07 | K12=未適用消費 × O1 | sealedSubclasses（直接継承者）と entries（末端フラット化）は集合も並びも不一致 | CrossModuleOrderTest#entriesDifferFromSealedSubclasses | — |
| XMP-08 | K12/K13=跨 module+MPP × O1 | 空階層は entries=[]・valueOf 常時失敗が跨 module/全ターゲットで一致 | CrossModuleOrderTest#emptyHierarchyIsObservable / BoundsTest#entriesIsEmptyOnEveryTarget / #valueOfAlwaysFailsUniformly / ConsumerApiTest#emptyHierarchyIsObservableAcrossModules | V5（旧 TC-GAP-018） |

## 2. else 無し when の跨 module 成立（V1-a/c/e）

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| XMP-09 | K12=未適用消費 × O4 | 全 public 階層で未適用側 kind-when が else 無し成立（V1-a）・全 9 種別末端も被覆 | CrossModuleWhenTest#kindWhenIsExhaustiveWithoutElse / #allLeafShapeKindsAreCovered | V1-a |
| XMP-10 | K12=適用消費 × O4 | 枝形 4 種（companion 等値/短縮形 `SI.Foo ->`/is 枝/object 等値）が網羅算入・中間 sealed 階層でも else 不要（V1-c） | PluginSideWhenTest#companionAndObjectEqualityBranches / #shorthandCompanionBranch / #isBranches / #intermediateHierarchyKindWhen | V1-c |
| XMP-11 | K12=未適用消費 × O4 | 値単位 when は sealed の地力で else 不要・スマートキャスト有効（V1 非依存） | CrossModuleWhenTest#valueWhenIsExhaustiveWithoutElse | — |
| XMP-12 | K12=未適用消費 × K10 × O4 | 階層内手動実装も inheritors 算入され is 枝が必要・entries には非出現・open 手動実装のサブタイプは inheritors 非追加（既存 is 枝が跨 module でも被覆）（V1-(e)） | CrossModuleWhenTest#manualImplementationBranchCounts | V1-(e) |
| XMP-13 | K12=未適用消費 × K8 × O4/O6 | else 要否は呼び出し位置依存: 不可視 kind を含む when は else 必須（省略は言語エラー）・else 枝で受ければ成立・internal 階層内手動実装を含む when も else 必須 | CrossModuleVisibilityTest#kindWhenOutsideVisibilityUsesElse / CrossModuleNegativeTest#elseIsRequiredForInvisibleKindsAndManualImpls | V1・可視性規則の正典=ケース02 |

## 3. 可視性境界と E-2 外部観測面

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| XMP-14 | K12 × K8 × O1 | 名指し不能な internal 末端各種別・private ネスト末端も entries/valueOf/label で観測可 | CrossModuleVisibilityTest#internalLeavesAppearInEntries / #valueOfResolvesInvisibleKinds / #privateNestedLeavesObservable | 3 段規則・壁の正典=ケース02 |
| XMP-15 | K12 × K8 × O6 | 全 internal 階層は跨 module で完全不可視（producer は成立・consumer は参照エラー）＝三者対比の下限 | CrossModuleNegativeTest#internalHierarchyIsInvisibleAcrossModule | — |
| XMP-16 | K12 × K8 × O1/O4 | 規則 2 フォールバック末端の asEnumish は生成 Enumish 型で返り・kind 突合せは entries/valueOf 同一性で成立 | CrossModuleVisibilityTest#asEnumishFallsBackToEnumishType | 規則 2 |
| XMP-17 | K12 × K8=E-2 × O1/O4/O6 | internal 基底+public class 末端: 値・kind API（asEnumish 規則 1 具体型/label/enumizedClass）は末端可視性で利用可・階層 API と基底 when は基底不可視で構成不能・具体型からの enumishCompanion 直接参照はコンパイル不能・具体型 companion を主語にした when は sealed 網羅が効かず else 要・成立は class 系末端のみ（境界は VIS-19 の NG 固定） | CrossModuleVisibilityTest#widerLeafValueAndKindApiWork / #widerLeafInheritedMembersResolve / #widerLeafLabelExtensionWorks / #autoCompanionWiderLeafWorks / CrossModuleNegativeTest#widerLeafBaseWhenCannotBeFormed | E-2・規則 3 発火=ケース04 |

## 4. 下流サブタイプ（非 final 末端の吸収）

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| XMP-18 | K12=下流サブタイプ × O1 | 別 module サブタイプ（abstract 直下/interface 実装=default asEnumish 継承/SAM ラムダ）は新 kind を作らず継承元 kind へ吸収・entries 集合と順序は不変 | DownstreamAbsorptionTest#subtypeIsAbsorbedIntoLeafKind / #interfaceImplementationIsAbsorbed / #samLambdaIsAbsorbed / #entriesSetAndOrderUnaffected | V10 |
| XMP-19 | K12=下流サブタイプ × O1 | enumizedClass は代表（末端自身）を返し実行時クラスと不一致 | DownstreamAbsorptionTest#enumizedClassReturnsRepresentative | V10 |
| XMP-20 | K12=下流サブタイプ × O4 | is 末端枝の値 when が下流サブタイプを被覆し else 無し網羅 | DownstreamAbsorptionTest#valueWhenCoversDownstreamSubtypes | V10 |

## 5. 適用消費と runtime-api 配線

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| XMP-21 | K12=適用消費 × O1 | 自前 @Enumize 階層と producer 生成 API が非干渉で共存（拡張登録・述語の独立） | OwnHierarchyCoexistTest#ownHierarchyWorksAlongsideProducerApi | — |
| XMP-22 | K12/K13 × O4 | 複数階層の Companion を `List<EnumishCompanion<Enumish>>` へ射影なし束ね（out 共変）・JVM 適用消費と common 全ターゲットの双方 | OwnHierarchyCoexistTest#companionsBundleWithoutProjection / RuntimeApiSurfaceTest#companionsBundleCovariantly | — |
| XMP-23 | K12=runtime-api 可視性 × O1 | api スコープ自動追加により未適用 consumer が runtime-api 型を明示宣言なし推移解決 | CrossModuleApiTest 内の型付き利用 / RuntimeApiExposureTest#consumerResolvesViaTransitiveApi | — |
| XMP-24 | K12=runtime-api 可視性 × O6 | 自動追加オプトアウト+implementation 隠しで producer は成功・consumer は supertype Enumish 未解決で失敗（api 公開必須の縮退対照） | RuntimeApiExposureTest#consumerFailsWhenRuntimeApiHidden | — |
| XMP-25 | K12=適用消費/K13=MPP × O1 | 適用側 consumer と MPP commonMain へ runtime-api が自動供給され klib として全ターゲット解決・producer の test compilation（src/test 内 @Enumize 階層）へは implementation 配線（KT-63142 警告回避）で供給されビルド成立 | OwnHierarchyCoexistTest#runtimeApiIsAutoSupplied / RuntimeApiSurfaceTest#runtimeApiTypesResolveFromCommon / SiContractTest（src/test の TestOnly 階層の成立が兼ねる） | opt-out 面=XMP-24 |
| XMP-26 | K12 × K10 × O1 | 基底 Enumish の自由実装は任意 module で許容・entries/valueOf に非出現・sealed 制約は生成 Enumish のみ | BaseEnumishFreeImplTest#baseEnumishImplementationIsUnrestricted | — |
| XMP-50 | K12 × K8=protected 基底 × O1/O4 | protected ネスト基底の階層 API（entries/valueOf/label）と else 無し kind-when が跨 module のサブクラス文脈で成立 | CrossModuleVisibilityTest#protectedBaseWorksInSubclassContext | 生成側の正典=ケース02 VIS-03 |

## 6. Java 消費と JVM バイナリ面

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| XMP-27 | K12=Java × O1 | Java は Companion static フィールド経由で entries/valueOf/valueOfOrNull と失敗文言を解決・@JvmStatic 非付与のため interface 上に static アクセサが存在しないことを reflection で固定 | JavaCompanionAccessTest#entriesAndValueOfResolveViaCompanionField / #valueOfFailureMessageIsObservable / #noStaticAccessorsExistWithoutJvmStatic | v1 制約の負値固定 |
| XMP-28 | K12=Java/K13=JVM × O1 | 共変 override の bridge がバイトコードに生成され Java から getLabel/getEnumizedClass/getEnumishCompanion/asEnumish が解決 | JvmBytecodeTest#covariantOverridesHaveBridges / JavaCompanionAccessTest#kindMembersResolveViaBridges | — |
| XMP-29 | K12=Java/K13=JVM × O1/O4 | jvmTarget17+ で基底と生成 Enumish に PermittedSubclasses が出力され Java 21 パターン switch が default 無し網羅（値階層・enum 末端階層とも）・生成 kind は Java enum でないため古典 enum switch は構成不能（NG 固定） | JvmBytecodeTest#sealedBaseHasPermittedSubclasses / #generatedEnumishHasPermittedSubclasses / JavaPatternSwitchTest#patternSwitchOverSealedValueIsExhaustive / #patternSwitchCoversEnumLeafHierarchy | V1 |
| XMP-30 | K12=Java × K3=enum × O1 | Java で name()="HELP" と kind getLabel()="Builtin" が管轄別に併存・全定数の asEnumish が同一 companion シングルトン | JavaEnumLeafTest#enumConstantNameAndKindLabelCoexist / #enumConstantsShareTheSingleKind | V4 |
| XMP-31 | K12/K13 × O1 | $EntriesHolder の搭載面: @Metadata 外・$ 名で Java 可視（ABI 保証外）・metadata klib 非搭載（生成宣言の搭載・inheritors の FQN 順直列化と併せて内容検査で固定）・跨 module 参照は producer 内に閉じる | JvmBytecodeTest#entriesHolderIsJavaVisibleWithDollarName / MppIcTest#metadataKlibCarriesDeclarationsWithoutEntriesHolder | V2-b・V5 |
| XMP-32 | K12=旧バイナリ差し替え × O1 | v1 でコンパイル→v2 jar で実行: entries は実行時版の集合・valueOf は新末端を実行時解決・旧 else 無し when は NoWhenBranchMatchedException（削除方向 v2→v1 は BLD-40） | BinarySwapTest#consumerCompiledAgainstV1SeesV2EntriesAtRuntime | 非 inline 検証とビルド駆動の正典=ケース06 |

## 7. MPP（全ターゲット・metadata・ソースセット・ランタイム特性）

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| XMP-33 | K13=jvm/js/wasmJs/wasmWasi/native(host) × O1/O4 | 生成 API 表面の全ターゲットパリティ: ゴールデン一括比較・kind シングルトン性・失敗文言同一・共変型付け・label 拡張・kind-when else 省略・simpleName のみ使用 | SiApiSurfaceTest#goldenSnapshotMatchesOnEveryTarget / #entriesAreKindSingletonsInFqnOrder / #valueOfFailsWithUniformMessage / #asEnumishReturnsSameSingleton / #labelExtensionMatchesKindLabel / #enumishCompanionIsCovariantlyTyped / #kindWhenIsExhaustiveWithoutElse | V8・JS の qualifiedName 不能は JsQualifiedNameTest が記録・通常 common 階層の成立自体が ON_* 非発火を含意（発火=ケース04） |
| XMP-34 | K13 × K3/K11 × O1 | 末端種別・型パラメータ挙動の全ターゲット一致: enum 管轄分離・value boxing・interface default asEnumish・SAM 維持・star projection・サブタイプ吸収 | EnumLeafTest / GenericTest / ValueBoxingTest / SamTest / AbsorptionTest | V4/V10/V8 |
| XMP-35 | K13 × K10 × O1 | toString 2 原則（生成/言語合成/手動/継承具象）の判定と表示が全ターゲット一致・Enumish 型経由の仮想ディスパッチ同一 | ToStringTest | V11 |
| XMP-36 | K13 × O3 | entries 順序（FQN 序数順・DFS break 順）が全ターゲットで一致 | OrderTest#entriesFollowFqnOrdinalOrder / #breakOrderIsIdentical | 順序値の正典=ケース03・metadata/platform バイト一致=ケース06 |
| XMP-37 | K13=metadata(common) × O1 | common @Enumize の生成宣言が metadata/klib に直列化され未適用 MPP 消費側の commonMain 利用関数・生成 companion 名指し・別 module JVM 参照が成立（gradle-plugin の metadata 含む全コンパイレーション適用を含意） | ConsumerApiTest#generatedApiWorksAcrossModules / #commonMainUsageFunctionsWork / #valueOfFailureMessageIsUniform / #generatedCompanionIsNameable | V5 |
| XMP-38 | K13 × K12 × O4 | klib の inheritors 直列化により跨 module kind-when が else 無し成立（object 末端=非 companion 経路・class/enum 末端=生成 companion 名指し経路） | ConsumerKindWhenTest#objectLeafKindWhenNeedsNoElse / #classLeafKindWhenNeedsNoElse / #enumLeafHierarchyKindWhenNeedsNoElse | V1-a × V5 |
| XMP-39 | K13 × O1 | common コードから生成 API を利用し default 実装ボディが platform 毎に充填・common の else 無し kind-when が全 platform 動作 | CommonUsageTest#commonFunctionSeesGeneratedApi / #commonKindWhenClassifiesWithoutElse / #enumishCompanionBodyIsFilledPerPlatform | V5 |
| XMP-40 | K13=HMPP 中間 × O1/O4 | 中間ソースセット（webMain）配置の階層が生成され js/wasmJs で動作・kind-when else 省略成立 | WebSharedTest#intermediateSourceSetHierarchyWorks / #kindWhenIsExhaustiveWithoutElse | 派生ソースセット逸脱の言語エラー=ケース04 |
| XMP-41 | K13=platform 専用 × O1/O4 | platform 専用ソースセット（jvmMain）の階層は V5 非依存で通常生成・when 網羅成立 | JvmOnlyHierarchyTest#platformOnlyHierarchyGenerates / #kindWhenIsExhaustive | V5 |
| XMP-42 | K13 × K12 × O1 | common 非 final 末端のサブタイプは platform ソースセットでも別 module klib でも吸収成立（sealed のソースセット制約は非 final 末端サブタイプに非該当） | JvmSubtypeTest#platformSourceSetSubtypeIsAbsorbed / #entriesUnchangedBySubtype / ConsumerSubtypeTest#subtypeIsAbsorbedAcrossModules / #interfaceLeafDefaultAsEnumishWorks | V10 |
| XMP-43 | K13 × K8=E-2 × O1/O4 | asEnumish 規則 1/2 と internal 基底の同一 module 利用が全 platform 一致・広い末端の値/kind API を MPP 未適用消費側から利用可 | VisibilityRulesTest#rule1UsesCompanionType / #rule2FallsBackToEnumishType / #internalBaseWorksInsideModule / ConsumerWiderLeafTest#valueAndKindApiUsableExternally / #labelExtensionWorksOnWiderLeaf | E-2（旧 TC-GAP-017）・規則 3 発火=ケース04 |
| XMP-44 | K13 × K8 × O1 | 参照不能 kind 5 態様の IR-only アクセサ（ネスト壁/トップレベル壁）が全 klib ターゲットで直列化・リンクを通過し entries/valueOf に掲載 | KindAccessorTest#inaccessibleKindsAreServedThroughEntries / #inaccessibleKindsResolvableByLabel | 壁 3 形の規則正典=ケース02 |
| XMP-45 | K13 × K15 × O1 | 遅延初期化の意味論一致: 反復アクセスで同一インスタンス（Native 新 MM の lazy・JS/Wasm 単一スレッドとも不変） | SiApiSurfaceTest#entriesReturnSameInstanceOnRepeatedAccess | — |
| XMP-46 | K13=JVM × K15 × O1 | 実マルチスレッド初回アクセス競合でも全スレッドが同一 entries（lazy SYNCHRONIZED） | LazyRaceTest#concurrentFirstAccessSeesSingleInstance | — |
| XMP-47 | K13=JVM × K15 × O1 | kind 初期化子からの entries 再入は JVM 実測で例外なし二重実行完了（他 platform は process-fatal 可能性・利用禁止事項は不変） | ReentrantInitTest#reentrantInitializerDoesNotOverflow | 再入経路の正典=ケース01 |
| XMP-48 | K13=js/wasm × O1 | DCE/リンク経路で $EntriesHolder へ通常参照で到達し entries 構築が成立（js/wasmJs/wasmWasi のテスト実行成立で実証） | SiApiSurfaceTest の js/wasmJs/wasmWasi 実行 | V2-b。明示 DCE 駆動・Native release DCE・Android R8 は保留.md PEN-04 |
| XMP-49 | K13 × O1 | 自作の reified inline ヘルパは intrinsic 書き換えされず全ターゲットで TODO へ到達（reified ヘルパ非提供方針の裏面） | CommonUsageTest#selfMadeReifiedHelperIsNotRewritten | V6 欠番・ヘルパ不存在の未解決エラー=ケース04 |
