# ケース06 ビルド動態

本資料は K14（ビルド動態）× O2/O5 の正典である。  
編集ケース #1〜#12（7-b/7-c 含む）・@Enumize 付与除去・決定性・基底不在ラウンド・
空⇔非空遷移・跨 module ABI 伝播・旧バイナリ差し替え・runtime-api 公開・MPP klib ラウンドを担う。  
実行時 OUT 照合（O1）は clean 一致判定の手段として併用する。  
他資料の IC・決定性に関する観測は本資料への参照で足りる（テスト戦略 §3 C4）。

## 1. 基準と決定性

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| BLD-01 | K14=clean×多ファイル分散 | clean ビルドで OUT 基準と全生成物ダイジェストを採取・`$EntriesHolder` クラスは基底ファイルの成果物に帰属（トップレベル基底 SI・ネスト基底 NB の両形） | IcRegressionTest#cleanBaselineCapturesOutputsAndEntriesHolderPlacement | P1・全編集ケースの比較基準・単一ファイル完結形は BLD-32 が担う |
| BLD-02 | 決定性=clean/UP-TO-DATE/from-cache | clean→無編集再実行=UP_TO_DATE→キャッシュ復元=FROM_CACHE で全生成物バイト一致（entries 順・valueOf 失敗メッセージを含意）・incremental 再生成の一致は編集→復帰系（BLD-04/07/28）が担う | DeterminismTest#cleanIncrementalAndFromCacheProduceIdenticalBytes | O5 正典・V7 |
| BLD-03 | 決定性=relocated | 別絶対パスへの複製ビルドが FROM_CACHE でヒットしバイト一致（relocatable キャッシュ） | DeterminismTest#relocatedBuildHitsCacheAndMatchesBytes | — |
| BLD-04 | 決定性=宣言順 | ファイル内宣言順の入替（中間 sealed 内部の並べ替え・階層内手動実装込み=inheritors の登録順非依存 FQN 正規化）で OUT・生成物バイト一致 | DeterminismTest#declarationReorderKeepsBytes | 順序内容の正典はケース03 |
| BLD-05 | 決定性=ロケール | 既定ロケールと tr-TR の clean 出力がバイト一致（collator・大小変換の非混入） | DeterminismTest#turkishLocaleBuildMatchesDefaultLocaleBytes | 'I'/'i' 特殊変換検出 |
| BLD-06 | 決定性×IR-only 生成 | toString（2 原則混在階層）と kind アクセサ（参照不能 kind 3 態様）を含む生成物でも BLD-02 と同じ 3 形態バイト一致 | DeterminismTest#cleanIncrementalAndFromCacheProduceIdenticalBytes / IcRoundsTest#accessorOutputsAreDeterministic | V11 |

## 2. 同一 module の編集ケース

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| BLD-07 | K14=#1 末端編集 | 末端へ private メンバー追記→OUT 不変・階層全ファイル共連れ再生成・編集対象以外バイト一致 | IcRegressionTest#case1LeafEditRegeneratesHierarchyWithIdenticalBytes | P1・V7 |
| BLD-08 | K14=#2 末端追加 | 新規ファイル末端追加→1 巡目は基底不在・else 無し kind-when の非網羅エラーで検出→枝追加で entries へ反映 | IcRegressionTest#case2LeafAdditionIsDetectedByWhenExhaustiveness | 本体網羅性エラー |
| BLD-09 | K14=#3 末端削除 | 末端ファイル削除→削除 kind の名指しがコンパイルエラー・stale 出力掃除→復帰で clean バイト一致 | IcRegressionTest#case3LeafRemovalSweepsStaleOutputsAndRestores | — |
| BLD-10 | K14=#4 基底のみ編集 | 基底コメント編集→末端側共連れ再生成も全出力バイト一致・独立階層非 dirty | IcRegressionTest#case4BaseOnlyEditKeepsLeafOutputBytes | V7 |
| BLD-11 | K14=#5 配置変更 | ファイル改名+宣言のファイル間移動→OUT・生成物不変（FQN 順正規化=物理配置非依存・ファイル改名はコンパイル順変動の観測を兼ねる） | IcRegressionTest#case5FileRenameAndDeclarationMoveKeepBytes | — |
| BLD-12 | K14=#6a 末端改名 | entries 並び再配置+label 変化・valueOf(旧名) 失敗・基底側生成物バイト変化（P1 実証） | IcRegressionTest#case6aLeafRenameReordersEntriesAndChangesLabel | label 変動規則 |
| BLD-13 | K14=#6b 外側改名 | 外側クラス改名→並びと enumizedClass のみ変化・label/valueOf 不変 | IcRegressionTest#case6bOuterRenameReordersEntriesKeepsLabel | — |
| BLD-14 | K14=基底改名 | 全ファイル一括の基底名変更→entries 不変・valueOf 失敗メッセージの基底名のみ変化 | IcRegressionTest#baseRenameKeepsEntriesAndChangesFailureMessage | — |
| BLD-15 | K14=#7 companion 増減 | 既定名 companion の手動追加/削除→OUT・`Foo$Companion` クラス名不変（自動生成⇔流用の透過・生成名は固定文字列で JVM マングリング無し）→復帰バイト一致 | IcCompanionTest#case7DefaultCompanionToggleKeepsShape | V3 |
| BLD-16 | K14=#7-b 名前付き切替 | `companion object Factory` 化→ABI 差分は修飾名指し側のみ・label/valueOf・短縮形 `Foo ->` 不変・旧 companion クラス stale 掃除 | IcCompanionTest#case7bNamedCompanionSwitchAffectsQualifiedReferencesOnly | — |
| BLD-17 | K14=#7-c 可視性トグル | internal 化=asEnumish 返り値型が規則 1(具体型)⇔規則 2(SI.Enumish) 切替で依存側型エラー→逆編集で復帰・private 化=規則 2 のまま IR-only アクセサで build 成立 | IcCompanionTest#case7cCompanionVisibilityToggleSwitchesReturnTypeAndAccessor | 3 段規則の分岐網羅(C2) |
| BLD-18 | K14=#7-c×広い末端 | 広い末端の companion internal 化→KIND_TYPE_NOT_DENOTABLE 発火→逆編集で解除（規則 3 の IC トグル） | IcCompanionTest#widerLeafCompanionNarrowingTogglesNotDenotable | E-2・KIND_TYPE_NOT_DENOTABLE |
| BLD-19 | K14=#8 label 衝突トグル | 同名末端の編集導入+別ファイル追加の 2 形で LABEL_CLASH 発火（報告先=当該ラウンドの当事者）→解消で clean バイト一致 | IcRegressionTest#case8LabelClashIntroductionAndResolution | LABEL_CLASH |
| BLD-20 | K14=#9 中間増減 | 中間 sealed 挿入+末端付け替え→入れ子展開順へ並び再計算→除去で復帰バイト一致 | IcRegressionTest#case9IntermediateSealedInsertionAndRemoval | — |
| BLD-21 | K14=中間単独編集 | 多段中間チェーンの非参照中間のみ編集（基底不在ラウンド）→OUT 不変・全生成物バイト一致 | IcRoundsTest#midOnlyEditKeepsOutputsWithoutBaseRound | V7 |
| BLD-22 | K14=#10+P3 | 無関係ファイル編集・独立階層の片側編集・新規 @Enumize 階層ファイル追加のいずれでも他階層の出力が再生成されない（双方向） | IcRegressionTest#case10UnrelatedEditDoesNotDirtyHierarchies / IcCompanionTest#newHierarchyAdditionDoesNotDirtyExisting | P3・V7（KT-55982/KT-75864 の税回避） |
| BLD-23 | K14=@Enumize 付与除去 | 除去→`SI$Enumish*`・自動 companion の stale 掃除・再付与+参照復元で clean バイト一致 | IcRegressionTest#enumizeToggleSweepsStaleAndRestoresBytes | — |
| BLD-24 | K14=#12 階層外手動実装 | `object Rogue : SI.Enumish` の追加ファイル単体ラウンドで MANUAL_IMPL_OUTSIDE_HIERARCHY 顕在化（共連れ不要）→削除で clean バイト一致 | IcCompanionTest#case12OutsideManualImplToggleRestoresBytes | MANUAL_IMPL_OUTSIDE_HIERARCHY・V1-(e) |
| BLD-25 | K14=階層内手動実装編集 | 末端を SI+SI.Enumish 両実装化→entries 不変・kind-when へ is 枝要求・非 ABI 編集で安定→復元で一致（共連れは #1 と同型） | IcCompanionTest#hierarchyInternalManualImplEditKeepsEntries | V1-(e) |
| BLD-26 | K14=末端の配置移設 | 末端をトップレベル→基底本体内へ移設→entries 並び変化・label/valueOf 不変（別外側クラスへの移設は FQN 変化＝並び再配置として BLD-12/13 と同型のため本形へ縮約） | IcRegressionTest#leafMoveIntoBaseBodyReordersEntriesKeepsLabel | 順序内容の正典はケース03 |
| BLD-27 | K14=基底可視性編集 | 基底可視性変更（公開面追随込み）→OUT・順序・末端側生成物バイト不変（生成宣言は常に public 宣言で実効可視性のみ外側追随＝バイト不変の根拠） | IcRegressionTest#baseVisibilityEditKeepsOrderAndOutputs | — |

## 3. ラウンド境界と粒度

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| BLD-28 | K14=基底不在ラウンド収束 | 連続編集+新規末端ファイル追加（基底非包含ラウンド）→各ラウンド green・成果物が clean 一致→削除で復帰 | IcRoundsTest#consecutiveEditsAndNewLeafFileMatchCleanBuild | P1 収束・1 巡目完走前提 |
| BLD-29 | K14=基底不在×診断 | 基底不在ラウンドで kind-when 非網羅エラー・同一ラウンド追加の同名 2 末端の LABEL_CLASH が発火（部分集合ビューで偽陽性なし） | IcRoundsTest#baseAbsentRoundReportsMonotonicDiagnostics | 診断単調性・LABEL_CLASH・KT-56588 追跡 |
| BLD-30 | K14=空⇔非空遷移 | 全末端削除→entries 空（valueOf 全失敗）→最初の末端追加で反映 | IcRoundsTest#emptyHierarchyBoundaryTransition | — |
| BLD-31 | K14=編集×参照不能 kind | 参照不能 kind 3 態様（private companion・別ファイル private TL 末端・private 外側ネスト末端）で末端編集の共連れ安定+新規末端の基底不在ラウンド収束 | IcRoundsTest#accessorEntriesSurviveEditsAndNewLeafRound | IR-only アクセサ |
| BLD-32 | K14=file-granularity | 同一ファイル 2 階層の片側編集→両階層がファイル単位で再生成されるがバイト一致 | DeterminismTest#sharedFileHierarchiesRegenerateTogetherKeepBytes | P3 は論理集約・物理は共連れ・V7 |
| BLD-33 | K14=toString の別ファイル依存 | 階層外親の具象 toString 除去/復元→kind 表示が label⇔継承表示へ追随（原則 2 の跨ファイル判定） | DeterminismTest#toStringFollowsSupertypeEditAcrossFiles | V11 |
| BLD-45 | K14=スコープ解決の跨ファイル編集 | typealias 付け替え（末端ファイル不変）→階層離脱・自動 companion の stale 掃除・同編集 clean と全生成物バイト一致→復元で基準一致。エイリアス宣言ファイルの張り替え→ENTRIES 不変・ファイル帰属差分を除きバイト一致。同一 pkg 囮の削除→incremental / clean の buildAndFail が同一の未解決エラー（失敗パリティ）→復元で基準一致 | IcRoundsTest#scopeResolutionEditsInOtherFilesMatchCleanBuild | V7 解決（設計00 §10 欠番）・候補判定の raw 追跡（設計01 §6） |

## 4. 跨 module ビルド動態

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| BLD-34 | K14=#11 跨 module 末端追加 | producer へ末端追加→未編集 consumer が再コンパイルされ else 無し kind-when が非網羅エラー→枝追加で entries 反映 | AbiPropagationTest#leafAdditionRecompilesConsumerAndReverifiesWhen | V1-b（inheritors 直列化+再検査） |
| BLD-35 | K14=跨 module 負値編集 | 末端削除=consumer の名指し未解決・@Enumize 除去=Enumish 未解決+producer の stale 掃除 | AbiPropagationTest#leafDeletionAndEnumizeRemovalFailConsumer | — |
| BLD-36 | K14=#7-c 跨 module | companion internal 化→consumer の具体型依存がコンパイルエラー→逆編集で復帰（返り値型切替の ABI 伝播） | CrossModuleIcTest#companionVisibilityTogglePropagatesReturnTypeAcrossModules | — |
| BLD-37 | K14=中間挿入の跨 module 波及 | internal 中間 sealed 挿入+末端付け替え→consumer の entries 並び変化・kind-when 不壊（波及の最小性） | CrossModuleIcTest#intermediateInsertionReordersEntriesAcrossModules | — |
| BLD-38 | K14=跨 module 決定性 | producer コメント編集→producer バイト一致・consumer UP-TO-DATE | CrossModuleIcTest#commentOnlyProducerEditKeepsConsumerUpToDate | — |
| BLD-39 | K14=下流サブタイプ追加 | consumer 側サブタイプ追加→producer UP-TO-DATE・バイト不変（entries 不変） | CrossModuleIcTest#downstreamSubtypeAdditionDoesNotDirtyProducer | V10 |
| BLD-40 | K12/K14=旧バイナリ差し替え | 追加方向: v1(2 末端) でコンパイルした consumer を v2(3 末端) jar で実行→entries=実行時集合・valueOf(新 label) 解決・v1 網羅 when は NoWhenBranchMatchedException・呼出側定数プールに getEntries 残存（非 inline）。削除方向: v2 でコンパイルした consumer を v1 jar で実行→entries=2 件・消えた label の valueOf は実行時 IllegalArgumentException | BinarySwapTest#consumerCompiledAgainstV1SeesV2EntriesAtRuntime / BinarySwapTest#consumerCompiledAgainstV2FailsValueOfOnV1Runtime | entries アクセサ非 inline が設計制約・klib 面は保留.md PEN-04 |
| BLD-41 | K12/K14=runtime-api 公開 | 既定=未適用 consumer が runtime-api 非宣言で生成 API を推移解決（api 自動追加）・addRuntimeDependency=false+implementation 隠しでは supertype Enumish 未解決で失敗 | RuntimeApiExposureTest#consumerResolvesViaTransitiveApi / RuntimeApiExposureTest#consumerFailsWhenRuntimeApiHidden | 正値/縮退の対 |
| BLD-46 | K14=#6a/#6b/#7-b の跨 module 波及 | 末端改名=consumer 名指し枝の失敗パリティ→追従後に並び+label 変化・旧 label の valueOf は実行時 IAE・同状態 clean と生成物一致。外側クラス改名=並びのみ変化・label 不変・valueOf 継続解決。companion の Factory 化=ABI 差分は修飾名参照側のみ buildAndFail・短縮形 `SI.Foo ->`・label・valueOf は不変。いずれも復元で基準一致 | CrossModuleIcTest#renameLeafPropagatesOrderAndLabelAcrossModules / #renameOuterKeepsLabelReordersEntriesAcrossModules / #namedCompanionSwitchPropagatesOnlyToQualifiedNamesAcrossModules | 同一 module 側の正典=BLD-12/13/16。跨 module 構成の from-cache/relocated は対象外（保留.md PEN-08 欠番） |

## 5. MPP ビルド動態

| ID | 次元/値 | 観測と期待 | 実装 | 備考(関連V/診断) |
|---|---|---|---|---|
| BLD-42 | K13/K14=klib ラウンド | 末端追加が jvm/js/wasmJs/metadata の全成果物（klib 含む）へ反映し clean と全一致（metadata と各 platform の生成宣言一致を含意・内容検査は XMP-31） | MppIcTest#leafAdditionReachesAllTargetsAndMatchesCleanBuild | V8・KT-62686=KGP 既定非 incremental で基底不在ラウンドなし |
| BLD-43 | K13/K14=metadata 診断 | 末端追加後、compileCommonMainKotlinMetadata 単独で else 無し kind-when が非網羅エラー | MppIcTest#leafAdditionBreaksCommonKindWhenExhaustiveness | O2 |
| BLD-44 | K13×決定性=MPP from-cache/relocated | MPP 全ターゲット（jvm/js/wasmJs/metadata）の from-cache 復元・relocated ビルドが clean とバイト一致 | MppIcTest#mppFromCacheAndRelocatedBuildsMatchBytes | V8。Native は保留.md PEN-04（ホスト依存） |
