# コーディングルール

## 全体共通ルール

- チャットへの返信は依頼に沿った言語で行え
- 返信文章は構造化して簡潔に行え。散文的にするな
  - 純粋な文章は日本語の場合2,000文字以下に抑えることを目標とする
  - 文章の他、表や図を使うなど、人間が読み取りやすいよう工夫する
- 実行環境がWindowsの場合も有る。コマンド実行前に確認すること
- コミットを発行する際は、なるべく変更内容の意味合い毎に分割すること
  - 勝手にpushすることは禁止

## ドキュメント・コメント更新ルール

- 議事録的な内容を残すことは禁止。あくまで事実と方針の記録のみ行え
- `markdown`は適切に改行せよ。同じ段落は半角スペース2つ、段落を変える際は改行2つで区切ること
- 記載内容は事実を保った範囲でなるべく端的・簡潔にせよ

## Kotlinを書く時のルール

- ライブラリバージョン等を勝手に更新することは禁止
- `import`無しでの外部定義利用は禁止（FQCNをコード中に直接書かない）
- クラスは1ファイルにつき1つまで
- 処理は意味単位で適切に関数分割すること
- 1クラス500行を超えないこと
- コンフィグ系など、必要性の無いデフォルト引数の利用は禁止
- 早期リターン処理を除き、1関数内の`return`は基本的に1つのみ定義可
- 変更を行った後は、ファイル名やコメント等の整合性まで確認し、全体の整合性を担保すること
  - 変更量を抑える表面的な対応より、全体の整合性を重視すること（単純追記だけで済ませない）
- デフォルト引数を使うことが意味の表現において最適でないなら利用しないこと（変更量を抑えるためだけのデフォルトは引数禁止）
- 内部のデータの流れを意識し、下流へ流すデータはなるべく状態が確定していること
  - 何度もフェッチやチェックが走らないよう、必要に応じて取得関数や値オブジェクトを導入する必要がある
- 値を比較するテストは、なるべく`data class`同士を1回の`assertEquals`で比較すること（個別プロパティ同士で比較しない）
