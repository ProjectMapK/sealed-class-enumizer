# sealed-class-enumizer

`sealed class` / `sealed interface` に enum 相当の操作系 API を生成する Kotlin (K2) コンパイラプラグイン。  
公開モジュールは `runtime-api` / `compiler-plugin` / `gradle-plugin` / `maven-plugin` の 4 つ。  
`integration-test/` はルートビルドに含まれない独立ビルドで、生成挙動とビルドツール上の適用を検証する。

## 資料の地図

- `docs/概要.md`: 利用者視点の仕様。設計資料と食い違えばこちらが正
- `docs/コンパイラプラグイン設計00〜02.md`・`docs/エッジケースへの対応方針.md`: FIR / IR / IC の設計と根拠
- `docs/test/テスト戦略.md` 以下: 結合テスト体系の正典。実装と資料が食い違えば実装が正
- `docs/実装ノート.md`・`docs/修正方針案.md`: 設計に無い実装事項と、仕様と実挙動の乖離。参照はこれらから他資料への一方向
- `README.md`・`CHANGELOG.md`: 利用者向け。CI の検査や記法など書き手向けの運用は書かない

## 検証コマンド

- ルートビルド: `./gradlew build`（単体テスト・`ktfmtCheck`・版表記の検査を含む）
- 整形: `./gradlew ktfmtFormat`（`integration-test` も対象。TestKit フィクスチャは診断の行番号を検証する入力なので整形しない）
- 結合テスト: `./gradlew -p integration-test build -x :gradle-integration:test -x :maven-integration:test`
- TestKit / Maven: `./gradlew -p integration-test :gradle-integration:test --rerun` / `:maven-integration:test --rerun`。  
  ローカル Maven の SNAPSHOT を入力に持ち UP-TO-DATE 判定が信用できないため、`--rerun` をタスク毎に必ず付ける
- 実行環境は Windows のこともある。改行は LF 固定であり、PowerShell の `>` / `Out-File` は CRLF を書くので
  作業ツリーのテキストへは使わない

## 作業の進め方

- チャットの返信は依頼と同じ言語で、構造化して簡潔に書く。地の文は日本語なら 2,000 文字以内を目安にし、表や図を使う
- **IMPORTANT**: コミット・push・PR 発行は明示の指示があるときだけ行う（「修正して」は作業ツリーへの修正の指示）。  
  main への直 push は禁止で、必ず PR を経る
- コミットは変更の意味合い毎に分割する
- ライブラリの版を変える必要があるときは、先に提案する
- CI ワークフローを変更したら、push する前にローカルで検証する（YAML のパース・`run` スクリプトの空撃ち・判定の期待値）
- 変更後はファイル名・コメント・資料まで整合させる。変更量を抑える表面的な追記より全体の整合性を優先する

## ドキュメントとコメント

- 現在の事実と方針だけを書き、経緯・撤回・判断の履歴は残さない
- コードを見れば分かる詳細（ソースの転載・クラスやテストの一覧・個数）は資料に書かず、役割と設計判断だけを書く
- 言語エラーで確実に止まる構成に対して、プラグイン診断の範囲を広げたり委譲の注記を資料へ足したりしない
- 上流モジュールのコード・コメントは下流に言及しない（言及自体が依存）。整合義務のコメントは下流側にのみ置く
- コード中のコメントから資料を参照するときは `docs/<ファイル名>.md §N` の完全パスで書く
- markdown の改行は 3 種を使い分ける。ハードブレイク（行末の半角スペース 2 つ）は文の区切りにのみ使い 1 文の中では使わない。  
  空行は段落分け、改行 1 つはファイル内の整形

## CHANGELOG のルール

- エントリは `## [Unreleased]` 配下へ `- #<PullRequestNumber>: <変更>` の形で書く
- 節は「利用者から使えるものが変わったか」で選び、
  `Added` → `Changed` → `Deprecated` → `Removed` → `Fixed` → `Security` → `Internal` の固定順で並べる
  - `Fixed`: インターフェースを変えずに挙動を正した変更。利用者から到達できない経路の修正は `Internal` に置く
  - `Internal`: 公開 API と挙動を変えないもの（内部リファクタ・性能改善・依存整理）。
    利用者に要るのは効率改善までで、内部リファクタは構造の変化を書かず「リファクタした」程度の要旨に留める
- 配布物に載らない変更（CI・ビルドスクリプト・テスト・docs）はエントリを書かない
- コメント・整形のみの PR は記載不要。その場合はコミットへ `Changelog: none` のトレーラを入れる

## Kotlin（`*.kt` / `*.kts`）

- `import` 無しで外部定義を使わない（FQCN をコード中に書かない）
- クラスは 1 ファイルに 1 つ。例外は同一 `sealed` 階層を構成するクラス群（末端・中間・吸収サブタイプ・kind の親クラス）で、
  ファイル名は基底の名前とし、配置自体に意味が無ければネストする
- クラスと同名のファイルに同居できるトップレベル宣言は、そのクラス専用の `private` ヘルパーと、
  そのクラスへの入口となる拡張プロパティ（セッションコンポーネントのアクセサ）だけ
- 複数のクラスから共有する、または単体で検証する宣言（関数・プロパティ・`typealias`・定数）は概念名のファイルへ切り出す。
  レシーバを持つものと `typealias` はトップレベルに置き、レシーバの無い関数と定数は 1 つでも `object` にまとめる。
  結合テストのフィクスチャは検証する形が優先で、この 2 項の対象外
- 1 クラスは 500 行に収める
- デフォルト引数はコンフィグ系など表現が適切な場合にのみ使う
- 早期リターンを除き、1 関数の `return` は 1 つ
- 下流へ流すデータは状態を確定させてから渡す。フェッチやチェックが繰り返されるなら取得関数や値オブジェクトを導入する
- 値を比較するテストは `data class` 同士を 1 回の `assertEquals` で比較する
- 結合テストのケース追加は、新しいフィクスチャ階層やテストクラスを立てる前に既存階層・既存テスト関数への合成で足りないかを検討する
- コメント 1 行は 100 文字以内に収める（ktfmt が長い行コメントを語の途中で折り返す）
- 仕上げに `./gradlew ktfmtFormat` を実行し、整形後の状態で動作確認まで行う
