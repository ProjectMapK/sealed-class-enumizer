# コーディングルール

## 全体共通ルール

- チャットへの返信は依頼に沿った言語で行え
- 返信文章は構造化して簡潔に行え。散文的にするな
  - 純粋な文章は日本語の場合2,000文字以下に抑えることを目標とする
  - 文章の他、表や図を使うなど、人間が読み取りやすいよう工夫する
- 実行環境がWindowsの場合も有る。コマンド実行前に確認すること
- CIワークフローを変更した場合、pushして試す前にローカルで検証すること
  - YAMLのパース・`run`スクリプトの空撃ち・判定の期待値確認まで行う
- コミットを発行する際は、なるべく変更内容の意味合い毎に分割すること
- コミットやpushは指示を受けてから行え
- 明示的指示が無い限りmainへの直pushは禁止。必ずPRを発行すること

## ドキュメント・コメント更新ルール

- 事実関係や現在の方針を残すことを重視し、過去の経緯を残すことは避けろ
- `markdown`の改行は次の3種を区別して使い分けよ
  - **改行**（ハードブレイク: 行末の半角スペース2つ + 改行）: 文を区切るために用いる。1文の中では用いない
  - **段落分け**（改行2つ = 空行1つ）: 段落を区切るために用いる
  - **内部改行**（ソフトブレイク: 改行1つ）: `.md`ファイル中の整形のために用いる。1文が長いようなケースでファイル内の見た目を整える目的で使う
- 記載内容は事実を保った範囲でなるべく端的・簡潔にせよ

## CHANGELOG のルール

- エントリは`## [Unreleased]`配下へ`- #<PullRequestNumber>: <変更>`の形で書く
- 節は「利用者から使えるものが変わったか」で選び、
  `Added` → `Changed` → `Deprecated` → `Removed` → `Fixed` → `Security` → `Internal`の固定順で並べる
  - `Added` / `Removed`: 公開API・生成物・対応ターゲットなど、利用者が触れる面の増減
  - `Changed`: 意図した挙動・既定値の変更（バグ修正ではないもの）
  - `Deprecated` / `Security`: Keep a Changelogの定義どおり。必要時のみ
  - `Fixed`: インターフェースを変えずに挙動を正した変更（特にバグ）
  - `Internal`: 公開APIと挙動を変えないもの。内部リファクタ・性能改善・依存整理
- 迷ったら「利用者のコード・ビルド結果・APIが変わるか」で判定し、変わらないなら`Internal`へ入れる
  - 実装上はバグ修正でも、利用者から到達できない経路の修正は`Internal`とする
    （`Fixed`は「利用者も踏んだかもしれない」と読ませるため）
- 配布物に載らない変更（CI・ビルドスクリプト・テスト・docs）はエントリを書かない
- 初回リリースのような「変更ではなく版そのものの説明」は節を作らず地の文で書く
- **CHANGELOGは利用者へ配るものであり、書き手向けの運用をファイル本文へ書かない**
  - この節の内容・CIの検査・トレーラの説明をCHANGELOGへ持ち込まない
- コメント・整形のみのPRは記載不要。その場合はコミットへ`Changelog: none`のトレーラを入れる

## Kotlin（`*.kt` / `*.kts`）を書く時のルール

- ライブラリバージョン変更が必要な場合、まず提案を行え
- `import`無しでの外部定義利用は禁止（FQCNをコード中に直接書かない）
- クラスは1ファイルにつき1つまで
  - 例外として、同一の`sealed`階層を構成するクラス群は1ファイルへまとめてよい
    - 末端・中間の他、末端の吸収サブタイプや`kind`の親クラスも同階層の構成要素として扱う
    - ファイル名は基底の名前とする
    - 配置（ネスト／トップレベル）自体が意味を持つ場合を除き、ネスト配置を優先する
- 処理は意味単位で適切に関数分割すること
- 1クラスは500行に収める
- デフォルト引数は、コンフィグ系など表現が適切となる場合にのみ利用する
- 早期リターンを除き、1関数内の`return`は基本的に1つのみ定義可
- 変更を行った後は、ファイル名やコメント等の整合性まで確認し、全体の整合性を担保する
  - 変更量を抑える表面的な対応より、全体の整合性を重視する（単純追記だけで済ませない）
- 内部のデータの流れを意識し、下流へ流すデータはなるべく状態を確定させた状態とする
  - 何度もフェッチやチェックが走らないよう、必要に応じて取得関数や値オブジェクトを導入する必要がある
- 値を比較するテストは、なるべく`data class`同士を1回の`assertEquals`で比較すること（個別プロパティ同士で比較しない）
- 仕上げとして必ず`ktfmt`によるフォーマッティングを実行すること
  - リポジトリルートで`./gradlew ktfmtFormat`を実行する。独立したビルドである`integration-test`もこれ1つで対象になる
  - フォーマット結果を含めて動作確認・コミットまで行う（未整形のまま完了報告しない）
  - `ktfmtCheck`は各ビルドの`check`に紐付いているため、未整形のコードはそのビルドの`build`で失敗する
  - `gradle-integration`のTestKitフィクスチャ（`src/test/resources/fixtures`）は診断の行番号を検証する入力であり、整形対象外とする
