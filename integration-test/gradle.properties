kotlin.code.style=official
org.gradle.caching=true

# 8 モジュールのうち依存関係の無いものを並行に構成・実行する（mpp-* の各ターゲットテストと
# gradle-integration の TestKit は互いに独立）
org.gradle.parallel=true
# 既定のデーモン設定は MPP 8 ターゲット分の構成と TestKit テストの並行実行には小さいため引き上げる
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g
